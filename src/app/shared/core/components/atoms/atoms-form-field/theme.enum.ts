export enum Theme {
}

export enum AlingIcon {
  right  = 'right',
  left  = 'left',
  default  = right,
}

export enum Direction {
  horizontal = 'radio-group',
  vertical = 'radio-group-vertical',
  default = horizontal
}
